rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: allows read/write access to the entire database for any authenticated user.
    // This is insecure for production but useful for rapid development.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
