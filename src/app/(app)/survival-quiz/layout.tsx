'use client';

// This is a basic layout for the survival quiz section.
// It can be expanded later if a specific layout is needed.
export default function SurvivalQuizLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
